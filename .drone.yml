kind: pipeline
type: docker
name: Test

platform:
  os: linux
  arch: amd64

steps:
- name: Build
  image: plugins/docker
  settings:
    repo: voidbuffer/emacs
    tags: [ latest ]
    dry_run: true
